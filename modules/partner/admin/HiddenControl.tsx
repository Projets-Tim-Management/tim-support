"use client";

/**
 * Composant vide : remplace un bouton natif du document (Save / SaveDraft /
 * Publish) pour le masquer. Toutes les actions passent par le menu 3-points
 * (voir PartnerClientEditMenu).
 */
export function HiddenControl() {
  return null;
}
